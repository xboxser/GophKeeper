# Конфигурация клиента

## Параметры

### Адрес и порт сервера

`env:"SERVER_ADDRESS"`  

Параметр по умолчанию:
`"a" "http://localhost:8018"`

### Адрес локального файлового хранилища для скачанных файлов с сервера

`env:"LOCAL_STORAGE"`  

Параметр по умолчанию:
`"ls" "../../upload_client/"`
